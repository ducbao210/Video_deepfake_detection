# @package _global_
model:
  name: video_swin

  architecture:
    backbone: swin3d_t       # swin3d_t | swin3d_s | swin3d_b
    pretrained: true
    freeze_backbone: false

  classifier:
    dropout: 0.3
    num_classes: 2